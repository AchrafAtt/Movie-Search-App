import { MovieResultsDirective } from './movie-results.directive';

describe('MovieResultsDirective', () => {
  it('should create an instance', () => {
    const directive = new MovieResultsDirective();
    expect(directive).toBeTruthy();
  });
});
